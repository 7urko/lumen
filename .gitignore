# dependencies
node_modules/
# next.js
.next/
out/
# core build output
core/dist/
# logs / os
*.log
.DS_Store
# env (ignore real env + secrets, keep *.example)
.env*
!.env.example
!.env*.example
# bundler real config holds a relay private key — never commit it
bundler/alto-config.json
bundler/.env

# scratch / one-off helpers (not part of the project)
_setup.bat
_webinstall.bat

# next.js generated
web/next-env.d.ts
_git*.bat
_gitcommit.bat
_gitinit.bat
